# File version: 2.4; date: 2026-05-30

services:
  - type: web
    name: intervention-sample-planner
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn intervention_sample_planner.web_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
